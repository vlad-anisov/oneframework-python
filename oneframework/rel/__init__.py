"""Реляционный слой: типизированное дерево -> параметризованный SQL."""

#: Ре-экспортов здесь нет намеренно: они держали бы питоновские `compile` и
#: `plan` -- вторую запись того, что делает ядро (`libs/js/src/core/rel/`), --
#: и держали бы **не ввозом**, а самим этим файлом.
